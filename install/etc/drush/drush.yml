command:
  sql:
    cli:
      options:
        extra: "--skip-ssl"
    connect:
      options:
        extra: "--skip-ssl"
    query:
      options:
        extra: "--skip-ssl"
    dump:
      options:
        extra: "--skip-ssl"
        extra-dump: "--skip-ssl"
        structure-tables-list: cache,cache_*,search_index,watchdog