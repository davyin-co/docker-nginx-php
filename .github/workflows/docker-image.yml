name: Docker Image CI

on:
  push:
    branches: [ main ]
  schedule:
    - cron:  '30 2 * * *'
  workflow_dispatch:
    inputs:
      name:
        description: 'Reason'
        required: true
        default: 'Why??'

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        arch:
          - 'alpine'
          #- 'debian'
        version:
          - '7.4'
          - '8.1'
          - '8.2'
          - '8.3'
        array: [
          {version: "7.4-alpine",  upstream_version: "7.4-alpine-7.7.8"},
          {version: "8.1-alpine",  upstream_version: "8.1-alpine-7.7.8"},
          {version: "8.2-alpine",  upstream_version: "8.2-alpine-7.7.8"},
          {version: "8.3-alpine",  upstream_version: "8.3-alpine-7.7.8"},
          {version: "7.4-alpine-7.7.7",  upstream_version: "7.4-alpine-7.7.7"},
          {version: "8.1-alpine-7.7.7",  upstream_version: "8.1-alpine-7.7.7"},
          {version: "8.2-alpine-7.7.7",  upstream_version: "8.2-alpine-7.7.7"},
          {version: "8.3-alpine-7.7.7",  upstream_version: "8.3-alpine-7.7.7"},
          {version: "7.4-alpine-7.7.6",  upstream_version: "7.4-alpine-7.7.6"},
          {version: "8.1-alpine-7.7.6",  upstream_version: "8.1-alpine-7.7.6"},
          {version: "8.2-alpine-7.7.6",  upstream_version: "8.2-alpine-7.7.6"},
          {version: "8.3-alpine-7.7.6",  upstream_version: "8.3-alpine-7.7.6"},
        ]

    steps:
      -
        name: Checkout
        uses: actions/checkout@v4
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      -
        name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}
      -
        name: Build and push
        uses: docker/build-push-action@v5
        with:
          push: true
          context: .
          platforms: linux/amd64,linux/arm64
          file: 'Dockerfile.alpine.template'
          tags: 'davyinsa/docker-nginx-php:${{ matrix.array.version }}'
          build-args: |
            PHP_VERSION=${{ matrix.array.upstream_version }}
